# =============================================================================
# VARIABLES DE ENTORNO - ARYNSTAL
# =============================================================================
# Copia este archivo a .env y configura los valores para tu entorno.
# NUNCA subas el archivo .env a git (ya está en .gitignore)
#
# Uso:
#   cp .env.example .env
#   nano .env  # Editar con tus valores
# =============================================================================

# -----------------------------------------------------------------------------
# DJANGO - Configuración básica
# -----------------------------------------------------------------------------

# Entorno: development o production
DJANGO_ENV=development

# Clave secreta (genera una nueva para producción)
# Puedes generar una con: python -c "from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())"
SECRET_KEY=tu-clave-secreta-muy-larga-y-aleatoria-aqui

# Modo debug (SIEMPRE False en producción)
DEBUG=True

# Hosts permitidos (separados por coma, sin espacios)
ALLOWED_HOSTS=localhost,127.0.0.1

# -----------------------------------------------------------------------------
# BASE DE DATOS - PostgreSQL (solo producción)
# -----------------------------------------------------------------------------

DB_NAME=arynstal
DB_USER=arynstal_user
DB_PASSWORD=tu-contraseña-segura
DB_HOST=localhost
DB_PORT=5432

# -----------------------------------------------------------------------------
# EMAIL - Configuración SMTP
# -----------------------------------------------------------------------------

# Para Gmail: smtp.gmail.com
# Para Brevo (ex-Sendinblue): smtp-relay.brevo.com
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_HOST_USER=tu-email@gmail.com
EMAIL_HOST_PASSWORD=tu-app-password

# Email desde el que se envían los correos
DEFAULT_FROM_EMAIL=Arynstal <info@arynstal.es>

# -----------------------------------------------------------------------------
# NOTIFICACIONES
# -----------------------------------------------------------------------------

# Email que recibe las notificaciones de nuevos leads
LEAD_NOTIFICATION_EMAIL=tu-email@ejemplo.com

# -----------------------------------------------------------------------------
# SEGURIDAD (solo producción)
# -----------------------------------------------------------------------------

# Forzar HTTPS
# SECURE_SSL_REDIRECT=True

# Cookies seguras
# SESSION_COOKIE_SECURE=True
# CSRF_COOKIE_SECURE=True

# HSTS (HTTP Strict Transport Security)
# SECURE_HSTS_SECONDS=31536000

# -----------------------------------------------------------------------------
# SENTRY - Monitoreo de errores (solo producción)
# -----------------------------------------------------------------------------
# Obtener el DSN en https://sentry.io después de crear un proyecto Django

# DSN de Sentry (obligatorio para activar monitoreo)
# SENTRY_DSN=https://xxx@yyy.ingest.sentry.io/zzz

# Porcentaje de transacciones a monitorear para performance (0.0 a 1.0)
# SENTRY_TRACES_SAMPLE_RATE=0.1

# Porcentaje de sesiones a perfilar (0.0 a 1.0)
# SENTRY_PROFILES_SAMPLE_RATE=0.1

# Entorno (production, staging, development)
# SENTRY_ENVIRONMENT=production
